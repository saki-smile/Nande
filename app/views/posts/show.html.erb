<div class="post-show">
  <h3><%= @post.title %></h3>
  <%= attachment_image_tag @post, :image, size: "500x350" %>
  <h4>ユーザーネーム：<%= @post.user.name %></h4>
  <div>
    <h4><ruby>気付<rt>きづ</rt>いたこと</h4>
    <p><%= @post.realize %></p>
  </div>
  <div>
    <h4><ruby>思<rt>おも</rt>ったこと</h4>
    <p><%= @post.think %></p>
  </div>
  <div>
    <h4><ruby>分<rt>わ</rt>かったこと</h4>
    <p><%= @post.understand %></p>
  </div>
  <div>
    <h4>まだ<ruby>分<rt>わ</rt>からないこと</h4>
    <p><%= @post.question %></p>
  </div>
  <h4>投稿日時：<%= @post.created_at.strftime('%Y/%m/%d') %></h4>
  <% if @post.user == current_user %>
    <%= link_to "編集", edit_post_path(@post) %>
    <%= link_to "削除", post_path(@post), method: :delete %>
  <% end %>
</div>